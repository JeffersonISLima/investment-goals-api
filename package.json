{
  "name": "investment-goals-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "prisma:generate": "DATABASE_URL=${DATABASE_URL:-postgresql://postgres:postgres@localhost:5433/investment_goals?schema=public} prisma generate",
    "prisma:migrate": "DATABASE_URL=${DATABASE_URL:-postgresql://postgres:postgres@localhost:5433/investment_goals?schema=public} prisma migrate dev",
    "prisma:deploy": "DATABASE_URL=${DATABASE_URL:-postgresql://postgres:postgres@localhost:5433/investment_goals?schema=public} prisma migrate deploy",
    "prisma:studio": "DATABASE_URL=${DATABASE_URL:-postgresql://postgres:postgres@localhost:5433/investment_goals?schema=public} prisma studio",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.0.0",
    "@prisma/client": "^6.18.0",
    "date-fns": "^4.1.0",
    "fastify": "^5.8.5",
    "fastify-type-provider-zod": "^6.1.0",
    "pg": "^8.13.1",
    "prisma": "^6.18.0",
    "zod": "^4.1.5",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@types/node": "^22.8.6",
    "@types/pg": "^8.15.6",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
